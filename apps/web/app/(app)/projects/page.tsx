import { ComingSoon } from '@/components/coming-soon';

export default function ProjectsStub() {
  return (
    <ComingSoon
      title="Projects"
      phase="Slice 1B"
      summary="Projects, phases, tasks, time logs, milestones, CRs. Per-project P&L with live margin."
    />
  );
}
