import { ComingSoon } from '@/components/coming-soon';

export default function TasksStub() {
  return (
    <ComingSoon
      title="My Tasks"
      phase="Slice 1B"
      summary="Assigned tasks across projects; daily standup updates; effort log feeds P&L."
    />
  );
}
