import { ComingSoon } from '@/components/coming-soon';

export default function UsersStub() {
  return (
    <ComingSoon
      title="Users & Roles"
      phase="Slice 1A (next sub-step)"
      summary="User list synced from Microsoft Graph; role + grade assignment editor."
    />
  );
}
