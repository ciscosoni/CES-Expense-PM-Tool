import { ComingSoon } from '@/components/coming-soon';

export default function TravelStub() {
  return (
    <ComingSoon
      title="Travel"
      phase="Slice 1C"
      summary="Travel requests with entitlement preview, active trips with GPS, closed trips with DA derivation."
    />
  );
}
