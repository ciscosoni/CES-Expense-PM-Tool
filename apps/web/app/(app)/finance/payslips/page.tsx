import { ComingSoon } from '@/components/coming-soon';

export default function PayslipsStub() {
  return (
    <ComingSoon
      title="Payslip Generation"
      phase="Slice 1E"
      summary="Every payslip line derived from a source record (DA day, reimbursement, etc.) — solves 'unexplained deductions' pain point."
    />
  );
}
