import { ComingSoon } from '@/components/coming-soon';

export default function ReportsStub() {
  return (
    <ComingSoon
      title="Reports"
      phase="Slice 1D"
      summary="Project P&L, resource utilization, travel spend, attendance summary — all xlsx-exportable."
    />
  );
}
